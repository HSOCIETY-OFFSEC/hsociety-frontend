/**
 * Endpoint override config.
 * Edit only this file when you need custom endpoint paths.
 * Keep values empty ("") to use the secure defaults.
 */

export const ENDPOINT_OVERRIDES = {
  AUTH: {},
  DASHBOARD: {},
  STUDENT: {},
  ADMIN: {},
  PUBLIC: {},
};

export default ENDPOINT_OVERRIDES;
