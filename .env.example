# Hsociety Frontend - Environment Variables
# Copy to .env.local for local development

# API Base URL
# Local dev: keep /api so Vite proxy forwards to localhost backend
# Netlify/Vercel production: set to https://hsociety-backend.onrender.com/api
VITE_API_BASE_URL=/api

# Optional overrides (defaults in env.config.js)
# VITE_API_TIMEOUT=30000
# VITE_SESSION_DURATION=86400000
# VITE_INACTIVITY_TIMEOUT=900000

# --- Live Threat Map ---
# Threat data provider: otx or abusech
# VITE_THREAT_PROVIDER=otx
# Insert your provider API key(s) in .env.local (never hardcode in source files)
# VITE_OTX_API_KEY=YOUR_OTX_API_KEY_HERE
# Polling interval in milliseconds
# VITE_THREAT_POLL_MS=15000
# Optional SOC destination used for arc endpoint on map
# VITE_SOC_LAT=40.7128
# VITE_SOC_LNG=-74.0060
# Optional backend proxy endpoint for threat feed (helps with CORS/secrets handling)
# VITE_THREAT_PROXY_URL=/api/threat-feed

# --- Server (used when running server from repo root or server/.env) ---
# PORT=3000
# FRONTEND_URL=http://localhost:5173
# MONGODB_URI=mongodb://localhost:27017/hsociety
